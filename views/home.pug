extends layout
block content
  h1 !{title}
  div(class="collection")
    each article, i in articles
      a( class="collection-item" href="/articles/"+article._id) #{article.title}
  a.btn(href="/articles/add") Dodaj nowy  
